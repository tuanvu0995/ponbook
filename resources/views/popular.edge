@layout('layouts/main')
@set('bodyClass', "director-show")

@section('body')
@!component("ui/hero", { title: title, type: "warning" })
<section>
    <div class="container  py-5">
      <h2 class="title is-5">{{ description }}</h1>
      @!component('partials/videoList', { videos: videos })
    </div>
  </section>
  <section>
    <div class="container  py-5">
      @!component('partials/pagination', { paginator: videos })
    </div>
  </section>
@end