@layout('layouts/admin-setting')
@set('title', "Information")

@section('breadcrumbs')
<li><a href="{{ route("admin.settings.index") }}">Settings</a></li>
<li class="is-active"><a href="#" aria-current="page">Information</a></li>
@end

@section('body')
<section>
    <div class="container is-fluid py-5">
        <div class="box">
            <h3 class="title is-4">Sitemap</h2>

                <div class="columns">
                    <div class="column is-6">
                        <p class="mb-2">Sitemap is a file that contains a list of all the pages on your website. It helps
                            search engines to find and index your website.</p>
                        <form action="{{ route("admin.settings.generateSitemap") }}" method="POST" class="py-5">
                            {{ csrfField() }}
                            @if(flashMessages.has('success'))
                            <div class="notification is-success">
                                <button class="delete"></button> {{ flashMessages.get('success') }}
                            </div>
                            @endif
                            <button type="submit" class="button is-primary">Generate Sitemap</button>
                        </form>
                    </div>
                </div>
            </div>    
    </div>
</section>
@end