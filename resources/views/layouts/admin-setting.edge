<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <link rel="icon" type="image/x-icon" href="{{ asset('assets/images/favicon.png') }}">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=0">
    <meta name="title" content="{{ title || "Ponbook" }} - Ponbook.net">
    <meta name="description" content="{{ description || "Ponbook" }}">
    <title>{{ title || "Ponbook" }} - Ponbook.net</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.css" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Quicksand:wght@300;400;500;700&display=swap" rel="stylesheet" type="text/css">
    <link rel="stylesheet" type="text/css" href="{{ asset('assets/app.css') }}">
    <script src="{{ asset('assets/js/jquery.min.js') }}"></script> 
    <script defer src="{{ asset('assets/common.js') }}"></script> 
    @!section('head')
    <style>
      html {
        overflow: hidden;
      }
    </style>
  </head>
  <body class="{{ bodyClass || '' }}">

    @include('partials/adminNavbar')

    <section class="my-5">
        <div class="container is-fluid">
            <nav class="breadcrumb" aria-label="breadcrumbs">
                <ul>
                    <li><a href="{{ route("admin.dashboard") }}">Dashboard</a></li>
                    @!section('breadcrumbs')
                </ul>
            </nav>
        </div>
    </section>

    <section style="min-height: 600px">
        <div class="container is-fluid py-5">
          <div class="columns">
            <div class="column is-3">
              <aside class="menu">
                <p class="menu-label">
                  General
                </p>
                <ul class="menu-list">
                  <li><a class="{{ request.matchesRoute('admin.settings.information') ? 'is-active' : '' }}" href="{{ route("admin.settings.information") }}">Information</a></li>
                  <li><a class="{{ request.matchesRoute('admin.settings.robots') ? 'is-active' : '' }}" href="{{ route("admin.settings.robots") }}">Robots</a></li>
                  <li><a class="{{ request.matchesRoute('admin.settings.sitemap') ? 'is-active' : '' }}" href="{{ route("admin.settings.sitemap") }}">Sitemap</a></li>
                </ul>
                <p class="menu-label">
                  Money Ads
                </p>
                <ul class="menu-list">
                  <li><a>Banner</a></li>
                  <li><a class="{{ request.matchesRoute('admin.settings.popUnder') ? 'is-active' : '' }}" href="{{ route("admin.settings.popUnder") }}">Popunder</a></li>
                  <li><a>Site</a></li>
                  <li><a>Link</a></li>
                </ul>
              </aside>
            </div>
            <div class="column is-9">
              @!section('body')
            </div>
          </div>
        </div>
      </section>
     
    @include('partials/footer')
    <script>
      const BASE_URL = '{{ env('APP_DOMAIN') }}';
    </script>
    <script src="{{ asset('assets/app.js') }}"></script>
    @!section('script-links')
    @!section('scripts')
  </body>
</html>