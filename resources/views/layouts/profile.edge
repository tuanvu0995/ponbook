<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <link rel="icon" type="image/x-icon" href="{{ asset('assets/images/favicon.png') }}">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=0">
    <meta name="title" content="{{ title || "Ponbook" }} - Ponbook.net">
    <meta name="description" content="{{ excerpt(description, 120) }}">

    @!include('partials/meta/keyword')
  
    <meta name="twitter:card" content="{{ description }}" /> 
    <meta name="twitter:site" content="ponbook.net" />
    <meta name="twitter:creator" content="Ponbook" />

    <title>{{ title || "Ponbook" }} - Ponbook.net</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.css" rel="stylesheet" defer/>
    <link href="https://fonts.googleapis.com/css2?family=Quicksand:wght@300;400;500;700&display=swap" rel="stylesheet" type="text/css" defer>
    <link rel="stylesheet" type="text/css" href="{{ asset('assets/app.css') }}" defer>
    <script src="{{ asset('assets/js/jquery.min.js') }}"></script> 
    <script defer src="{{ asset('assets/common.js') }}"></script> 
    @!section('head')
    <style>
      html {
        overflow: hidden;
      }
    </style>
  </head>
  <body class="has-navbar-fixed-top">

    @include('partials/navbar')

    <div class="container py-5" style="min-height: 600px">
      <div class="columns">
        <div class="column is-2">
          <div class="box">
            <div class="media">
              <div class="media-left">
                <figure class="image is-48x48">
                  <img src="https://bulma.io/images/placeholders/96x96.png" alt="Placeholder image">
                </figure>
              </div>
              <div class="media-content">
                @if(profile.fullname)
                <p class="title is-4">{{ profile.fullname }}</p>
                @end
                <p class="subtitle is-6">{{ `@${profile.username}` }}</p>
              </div>
            </div>
          </div>
        </div>
        <div class="column is-8">
          @!section('body')
        </div>
        <div class="column is-2">
          RIGHT
        </div>
      </div>
    </div>
     
    @include('partials/footer')
    <script>
      const BASE_URL = '{{ env('APP_DOMAIN') }}';
    </script>
    <script src="{{ asset('assets/app.js') }}"></script>
    @!section('script-links')
    @!section('scripts')
  </body>
</html>